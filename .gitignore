# Only the connector module is tracked in this repo.
# Ignore everything at the root, then re-include what we want.
/*
!/.gitignore
!/sdlc_woo_connector/

# Python cache/build artifacts (also inside the module)
__pycache__/
*.py[cod]
*$py.class

# Local editor/OS artifacts
.DS_Store
Thumbs.db
